# 使用指南

最短路径：进入项目目录，运行 `bash wechat.sh web`，在网页填写主题、原文、赛道、立场和目标读者，然后提交。

系统会连续生成仿写稿、40 个标题和人味终稿。终稿完成后就可以复制正文或切换标题；配图是独立阶段，候选加载失败也不影响文章。

任务文件统一在 `output/jobs/任务编号/`。若使用手机，只是通过 SSH 远程提交命令；文章仍保存在 Mac，不会同步到任何第三方笔记库。

完整安装、赛道、配置、配图和测试说明见项目根目录的 `README.md`；终端流程见 `TERMINAL_WORKFLOW.md`。
