# 使用指南

最短用法：进入项目目录，先运行 `bash wechat.sh doctor`，再运行 `bash wechat.sh web`。在网页填写主题、参考原文、赛道、你的立场和目标读者，然后提交。

系统依次完成仿写与联网核对、40 个标题、人味终稿。完成后可以复制正文或切换标题。当前版本没有配图、自动发布和 Obsidian 同步。

默认模型是 Codex `gpt-5.6-sol`，推理强度为 `high`。要使用 Claude、豆包、DeepSeek 或其他兼容接口，先运行 `bash wechat.sh configure` 写入本机密钥，再在网页选择模型配置。密钥不会显示在网页或日志中。

每个任务都在 `output/jobs/任务编号/`。手机只通过 SSH 远程提交，文章仍在电脑生成和保存；断开手机不会停止任务。

完整配置与测试见 `README.md`，终端组织方式见 `TERMINAL_WORKFLOW.md`。
