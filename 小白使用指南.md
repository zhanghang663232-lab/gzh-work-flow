# 小白使用指南

1. 打开终端，进入这个项目文件夹。
2. 输入 `bash wechat.sh web`，浏览器会打开工作台。
3. 选择赛道，填写要写的主题、参考原文和你的态度。
4. 点击提交，等待任务完成。
5. 在页面复制终稿，从 40 个标题里选择一个；如需图片，再勾选候选图片并点击下载。

文章不会直接发到公众号，也不会自动下载任何图片。每篇文章会保存在 `output/jobs/任务编号/`。

如果页面打不开，先在终端运行 `bash wechat.sh doctor`，再按提示补齐 Codex CLI、Python 或配图依赖。
