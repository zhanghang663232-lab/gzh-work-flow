# 小白使用指南

1. 打开终端，进入这个项目文件夹。
2. 第一次使用先输入 `bash wechat.sh doctor`，看默认 Codex 是否显示“已配置”。
3. 输入 `bash wechat.sh web`，浏览器打开终端显示的本机地址。
4. 选择赛道，填写主题、参考原文、你的态度和目标读者。
5. 点击“提交，一次跑完”。网页可以关闭，电脑不要关机或断网。
6. 完成后，从 40 个标题中选一个，再复制正文或“标题和正文”。

文章不会自动发布，不会下载图片，也不会同步到 Obsidian。每篇文章保存在 `output/jobs/任务编号/`。

如果想用 Claude、豆包或 DeepSeek，先输入 `bash wechat.sh configure` 保存 API Key。Key 只留在本机私密文件中。然后重新打开网页选择模型。

如果页面打不开或模型不可用，再运行一次 `bash wechat.sh doctor`，按照“未配置”提示处理。
